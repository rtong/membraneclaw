{
  "workflow_id": "swro-watertap-compact",
  "version": "0.3.0",
  "call_budget": {"simulate_ro_max": 5, "simple_arithmetic_tool_allowed": false},
  "states": [
    {"id": "preflight", "required": ["decision_variable", "fixed_inputs", "constraints", "requested_outputs"]},
    {"id": "derive_control", "required": ["minimum_flux_or_active_quality_limit"]},
    {"id": "baseline", "required": ["complete_arguments", "echo_check", "baseline_margin"]},
    {"id": "bounded_candidates", "required": ["fixed_inputs_unchanged", "feasible_infeasible_evidence"]},
    {"id": "unrounded_final_check", "required": ["all_constraints_pass", "requested_outputs_complete"]},
    {"id": "single_final_report", "required": ["six_sections", "no_followup_tool_call", "no_repetition"]}
  ],
  "recipe_requirements": {
    "pressure_selection": ["minimum_flux", "first_deficit", "two_point_slope", "direct_final_verification"],
    "membrane_degradation": ["rejection_derived_limit", "strict_active_limit", "normal_margin", "unrounded_boundary_check"]
  }
}
