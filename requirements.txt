# Direct dependencies. Install with:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# requirements.lock.txt holds the full resolved set from a known-good environment.

qwen-agent[rag,code_interpreter]==0.0.34

# qwen_agent.utils.utils imports soundfile unconditionally but never declares it.
soundfile==0.14.0

# OpenAI-compatible front end (server.py)
fastapi==0.140.0
uvicorn[standard]==0.51.0

# MCP client, for reaching the WaterTAP RO server (agent/core.py)
mcp>=1.2

# Verifies the identity JWT Open WebUI forwards (agent/identity.py). Already
# arrives transitively via mcp, but this file depends on it directly.
pyjwt>=2.8
